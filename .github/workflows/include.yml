---
name: Execute test on node
on:
  workflow_call:
    inputs:
      branch_name:
        type: string
        description: 'Name of git branch'
        required: false
        default: ''


jobs:

    run_test:
      runs-on: ubuntu-latest
      steps:

        - name: Show files
          run: exit 1

        - name: Show files
          if: failure()
          run: echo "step one"

        - run: echo "step 2"
          if: always()

        - name: Show files
          if: failure()
          run: echo "step 3"